# design-refs

Drop design screenshots or exports here (PNG/JPG). The comic-pop system was
built from the brief; add reference frames here and they can be matched
precisely — colours, type scale, spacing, component treatments.

Suggested naming: `hero.png`, `card.png`, `search-bank.png`, etc.
